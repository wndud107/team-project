<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head')%>
    <link rel="stylesheet" href="styles/my-board.css">
</head>
<body>
    <%- include('includes/header')%>
    <h1>내가 쓴 글</h1>
    
    <div class="board-list">
        <% if (posts.length > 0) { %>
            <% for (let i = 0; i < posts.length; i++) { %>
                <div class="board">
                    <h4><a href="/<%= posts[i].board %>-board"><%= posts[i].board_name %></a></h4>
                    <h3><a href="/<%= posts[i].board %>-content/<%= posts[i]._id %>" action="/<%= posts[i].board %>-content/:<%= posts[i]._id %>"><%= posts[i].title %></a></h3>
                    <div class="low-element">
                        <p>댓글 수: <%= posts[i].commentCount %></p>
                        <span>|</span>
                        <p>작성일: <%= new Date(posts[i].date).toLocaleDateString("ko-KR") %></p>
                    </div>
                </div>
            <% } %> 
        <% } else { %>
            <style>.board-list {border-bottom:  1px solid rgb(11, 154, 167);};</style>
            <p>작성한 글이 없습니다.</p>
        <% } %>
    </div>
</body>
</html>

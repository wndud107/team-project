<!DOCTYPE html>
<html lang="ko">
<head>
    <%- include('includes/head')%>
    <link rel="stylesheet" href="styles/update-board.css">
</head>
<body>
    <%- include('includes/header')%>
    <div class="container">
        <%- include('includes/aside')%>
        <main class="main-content">
            <h1>오운완게시판 글쓰기</h1>
            <form action="/save-end-board" method="post">
                <input type="text" name="title" placeholder="제목을 입력해 주세요." class="title-input">
                <input type="file" name="file" placeholder="파일" class="file-input">
                <textarea placeholder="내용을 입력해 주세요." name="content"></textarea>
                <button type="submit">등록</button>
            </form>
        </main>
    </div>
    <script>
        document.getElementById('file-input').addEventListener('change', function() {
            var fileName = this.files[0] ? this.files[0].name : '선택된 파일 없음';
            document.querySelector('.file-name').textContent = fileName;
        });
    </script>
</body>
</html>
